package com.sahyadrisiri.ui.theme

import androidx.compose.ui.graphics.Color

// ── iOS system colors (exact hex matches from App.tsx) ─────────────────────
val Blue007AFF   = Color(0xFF007AFF)   // Primary blue
val Green34C759  = Color(0xFF34C759)   // Clean / success
val RedFF3B30    = Color(0xFFFF3B30)   // Polluted / danger
val OrangeFF9500 = Color(0xFFFF9500)   // Warning

// ── Status background tints ────────────────────────────────────────────────
val CleanBg      = Color(0xFFE5F5EA)
val WarnBg       = Color(0xFFFFF4E5)
val PollutedBg   = Color(0xFFFFEBE5)

// ── Neutral / scaffold ──────────────────────────────────────────────────────
val Background   = Color(0xFFF2F2F7)   // #f2f2f7
val CardWhite    = Color(0xFFFFFFFF)
val Neutral100   = Color(0xFFF2F2F7)
val Neutral200   = Color(0xFFE5E5EA)
val Neutral400   = Color(0xFF8E8E93)
val Neutral500   = Color(0xFF6C6C70)
val Neutral600   = Color(0xFF48484A)
val Neutral800   = Color(0xFF2C2C2E)
val Black        = Color(0xFF000000)
